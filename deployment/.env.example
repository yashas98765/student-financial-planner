# Optional overrides for deployment/docker-compose.prod.yml

# Use managed MongoDB if available
# MONGODB_URI=mongodb+srv://<user>:<pass>@<cluster>/<db>

# Public URL used by backend CORS allow-list
# FRONTEND_URL=http://localhost
